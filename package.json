{
  "name": "nlpdates",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && node esbuild.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "charliezstong",
  "license": "ISC",
  "devDependencies": {
    "chrono-node": "^2.7.3",
    "esbuild": "^0.19.8"
  }
}
